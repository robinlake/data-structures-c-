/*
A doubly linked list has a pointer from each node to both the next and the previous node in the list.

Some operations where the time is different than a single linked list:
PopBack():
 - takes O(1) time
AddBefore(node, key)
 - takes O(1) time


*/